export const publicRoutes = ["/", "/products", "/about"];
export const authRoutes = ["/auth/login", "/auth/register"];

export const apiAuthPrefix = "/api/auth";
export const adminRoutesPrefix = "/admin";

export const DEFAULT_REDIRECT_URL = "/";
